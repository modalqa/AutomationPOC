/// <reference types="jest" />
import { IJestStareConfig } from "./doc/IJestStareConfig";
import { Logger } from "../utils/Logger";
import { IProcessParms } from "./doc/IProcessParms";
export declare class Processor {
    private mResults;
    private mExplicitConfig?;
    private mProcessParms?;
    static run(results: jest.AggregatedResult, explicitConfig?: IJestStareConfig, parms?: IProcessParms): jest.AggregatedResult;
    private mLog;
    constructor(mResults: jest.AggregatedResult, mExplicitConfig?: IJestStareConfig, mProcessParms?: IProcessParms);
    private generate;
    private collectImageSnapshots;
    private generateReport;
    private execute;
    private addThirdParty;
    private obtainWebFile;
    private obtainJsRenderFile;
    logger: Logger;
}
